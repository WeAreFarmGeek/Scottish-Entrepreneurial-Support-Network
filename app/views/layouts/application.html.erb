<!DOCTYPE html>
<html>
<head>
  <title>Scottish Entrepreneurial Support Network</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <script src="http://d3js.org/d3.v3.min.js" charset="utf-8"></script>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="logo">
  <a href="/" data-no-turbolink="true">
    <%= image_tag Header.first.image.url if Header.first %>
    <div class="header">
      <h1><%= Header.first.title if Header.first %></h1>
      <h2><%= Header.first.subtitle if Header.first %></h2>
    </div>
  </a>
</div>
<div style="clear: both;"></div>

<% if flash[:alert] %>
  <p class="flash error"><%= flash[:alert] %></p>
<% end %>

<% if flash[:notice] %>
  <p class="flash notice"><%= flash[:notice] %></p>
<% end %>

<%= yield %>

<div class="footer">
  <h3>Our Sponsors</h3>
  <small>Copyright <%= Time.now.year %> <a href="http://power-of-youth.org">Power of Youth</a>. All rights reserved.</small>
  <div style="clear: both;"></div>
  <hr />
  <%= image_tag Footer.first.image.url if Footer.first %>
  <% if ENV['GOOGLE_ANALYTICS_KEY'] && ENV['GOOGLE_ANALYTICS_DOMAIN'] %>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
       (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
       m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
       })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', '<%= ENV['GOOGLE_ANALYTICS_KEY'] %>', '<%= ENV['GOOGLE_ANALYTICS_DOMAIN'] %>');
      ga('require', 'linkid', 'linkid.js');
      ga('send', 'pageview');
    </script>
  <% end %>
</div>

</body>
</html>
